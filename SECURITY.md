# 安全策略

## 攻击面声明

Arch Lens 是运行在本地 DSH 进程内的插件：

- **无服务端、无网络监听**：除宿主 DSH 已配置的模型 API 调用外，不发起任何网络请求；
- **数据不出机器**：扫描结果、代码索引、笔记、文档、缓存全部只写入被扫描的本地工作区；
- **凭据不外露**：本插件不接触模型 API key，LLM 调用完全经由宿主的会话/模型管线。

需要留意的既有边界：

- 「AI 生成」经主会话管线执行，**遵守宿主的工具审批体系**——插件本身不绕过审批；
- 工作区内的 `.arch-lens-*.json` 缓存与 `ARCH-NOTES.md` 是明文产物，请按你仓库的保密基线对待
  （例如不要把含敏感结论的笔记文件提交到公开仓库）。

## 报告漏洞

发现漏洞请通过仓库的 **GitHub 安全咨询（Private security advisory）** 报告，
或邮件至 `1294642794@qq.com`。
我们会尽快响应确认（通常 72 小时内），修复发布前不公开细节。
